node_modules
.pnpm-store
.turbo
dist
.next
out
coverage
*.log
.DS_Store
.env
.env.local
.env.*.local
*.tsbuildinfo

# Runtime state — local to each machine, contains trade history with PnL
apps/market-worker/logs/
apps/market-worker/data/

# Data-collector runtime artifacts (large captured WS data; 300 MB per 30h run)
apps/data-collector/logs/
apps/data-collector/collector-*.pid

# Autoresearch runtime/output state (per-trial configs and loop state grow
# rapidly; the source scaffolds — instructions.md, launch_prompt.md,
# score.py — are checked in alongside).
autoresearch_*/state/
autoresearch_*/configs/

# Per-machine temp data / staging
*.pre-recal-*
*.backup-*

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo

# Python cache
__pycache__/
*.py[cod]
